/// Application-level constants
import 'package:flutter/material.dart';

class AppConstants {
  /// Application name
  static const String appName = 'muslim_guide';

  /// API endpoints
  static const String baseUrl = 'https://api.example.com';

  // ================= COLORS =================

  /// Primary green (navbar, buttons)
  static const Color primaryColor = Color(0xFF2F6F5E);

  /// Secondary gradient green
  static const Color secondaryColor = Color(0xFF4FAF8F);

  /// Light background green (cards)
  static const Color lightGreen = Color(0xFFE8F3EF);

  /// Accent green
  static const Color accentGreen = Color(0xFFA7D7C5);

  /// Main background color
  static const Color backgroundColor = Color(0xFFFFFFFF);

  /// Primary text color
  static const Color textPrimary = Color(0xFF1C1C1C);

  /// Secondary text color
  static const Color textSecondary = Color(0xFF6B6B6B);
}